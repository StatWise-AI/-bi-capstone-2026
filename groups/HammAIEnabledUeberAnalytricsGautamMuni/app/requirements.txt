# --------------------------------------------------------------------------
# Application dependencies (unchanged from the original project)
# --------------------------------------------------------------------------
Flask>=3.0
pandas>=2.0
openpyxl>=3.1
python-dotenv>=1.0
google-genai>=1.0

# --------------------------------------------------------------------------
# Production WSGI servers
# --------------------------------------------------------------------------
# gunicorn: used on Render (Linux). This is what render.yaml / Procfile
# actually invoke in production: `gunicorn wsgi:app --bind 0.0.0.0:$PORT`
gunicorn>=21.2

# waitress: optional, Windows-compatible production server. Not used by
# Render, but included so the same requirements.txt can be used to test a
# production-style run locally on Windows with:
#   waitress-serve --host=0.0.0.0 --port=5000 wsgi:app
waitress>=2.1
